%p
  %span Total records:
  = paginated_records.records.count

- paginated_records.pages.each do |number|
  = link_to number, redirect_method.call(params.permit(:page, :name, :completed_at_start, :completed_at_end).merge(page: number))
